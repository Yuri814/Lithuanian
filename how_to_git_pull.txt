git status                          # 어떤 파일이 변경/추가되었는지 확인
git add file's_name.py              # 특정 파일만 추가하는 경우 'file's_name.py' 부분 수정
git add .                           # 또는 모든 변경사항을 추가하려면:
git commit -m "commit comment"      # comment 내용용
git push